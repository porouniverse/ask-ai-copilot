阅读"/src/main.js"，然后优化：

（1）main.js设计上应该关注的是electron的架构等内容比如`app.whenReady()` 、 `app.on`等，但是目前performAutomation及其调用链的方法都写在了这个js上，我希望将它们抽取为一个单独的js文件，main.js只负责调用performAutomation即可；



（2）performAutomation目前只处理了`config.sites[0]`一种情况，需要改造为遍历config.sites，每个item都是同样的操作步骤；当然拷贝user_data_copy_for_playwright是公共操作，注意提取为单独的调用；